# Docker 侧环境变量(复制为 .env 后 docker compose 自动读取)
# HuggingFace 模型下载源;国内建议使用镜像加速:
HF_ENDPOINT=https://hf-mirror.com

# 可选:覆盖默认嵌入模型
# TEXT_EMBEDDING_MODEL=BAAI/bge-small-zh-v1.5
# IMAGE_EMBEDDING_MODEL=OFA-Sys/chinese-clip-vit-base-patch16
